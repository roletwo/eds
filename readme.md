# Exponential Distribute Share

```text
eds <command>

Commands:
  cli.ts poll_cut <share> <member> <ratio>  Calculate all member shares as a
                                            list from a poll list
  cli.ts cut <share> <member> <ratio>       Calculate all member shares as a
                                            list
  cli.ts calc_min <share> <member> <ratio>  Calculate minimal member share

Options:
  --help     Show help                                                 [boolean]
  --version  Show version number                                       [boolean]
```
